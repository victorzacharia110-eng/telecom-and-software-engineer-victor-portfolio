[build]
  command = "npm run build"
  publish = "dist"

# Single-page app fallback: vue-router uses HTML5 history mode,
# so all unmatched routes must serve index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
