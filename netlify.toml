[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: vue-router uses HTML5 history mode, so all routes
# must serve index.html and be resolved client-side.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
