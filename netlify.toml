[build]
  command = "npm run build"
  publish = "dist"

# Vue Router uses HTML5 history mode, so all client-side routes
# must fall back to index.html for direct visits / refreshes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
